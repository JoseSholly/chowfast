# railway.toml

# Explicitly tell Railway to use the DOCKERFILE builder
[build]
builder = "DOCKERFILE"

# Specify the path to your Dockerfile (assuming it's in the root)
dockerfilePath = "Dockerfile" 

# OPTIONAL: You may also want to set the start command explicitly
# [deploy]
# startCommand = "/entrypoint.sh"